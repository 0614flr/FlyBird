using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CameraConturoler : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Application.
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
